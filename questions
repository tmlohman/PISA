Questions:

1. If I want to have multiple sections of the page (graph, text, scatter), 
how should I set this up? Divs? Body tag?
2. Significance of the svg variable. Is the name important? Can I have another one?
and .append('g') what is the g?
3. Why is the map variable declared separately? Could it all be done in 
one step with svg? Advantages/disadvantages?
4. Best practices for organizing asynchronous programs? Tools for visualizing 
the control flow?
5. Explanation of why I can't just re-draw the whole thing every time...
re-declaring svg in pick_color function.



44733

If time:
1. Accessing external libraries
2. Formatting hovertext, simply


			// draw axis for scatter-plot
			var x = d3.scale.linear().range([2*width/3, width]),
				y = d3.scale.linear().range([height/2, height]),
				x_axis = d3.svg.axis().scale(x),
				y_axis = d3.svg.axis()
						   .scale(y)
						   .orient('left');
			
			var scatter = svg.selectAll('circle')
										   
			  d3.select("scatter")
				.append('s')
				.attr('class', 'x axis')
				.attr('transform', "translate(0," + height + ")")
				.call(x_axis);
				
			  d3.select("scatter")
				.append('s')
				.attr('class', 'y axis')
				.attr('transform', "translate(" + margin + ",0)")
				.call(y_axis);
